# Serving dependencies only. scikit-learn and xgboost must match the
# versions the model was pickled under — a joblib artifact loaded by a
# different minor version emits warnings and can silently change results.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
joblib==1.4.2
shap==0.46.0
matplotlib==3.9.2
